# URL base de la API para el build de producción.
# Relativa (/api/v1) => mismo origen que sirve el frontend (https://98.94.193.64),
# así nginx enruta /api/ a Django. Sin mixed-content ni CORS.
VITE_API_BASE_URL=/api/v1
